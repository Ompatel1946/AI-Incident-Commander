Incident ID : Q39OZFTVFHYSEF
Title       : payment-service OOMKilled repeatedly — memory leak after stripe-python upgrade
Urgency     : high
Service     : payment-service
Started     : 2026-05-30T06:44:22Z
Generated   : 2026-05-30T06:44:56.938757+00:00
============================================================

INCIDENT SUMMARY:
The payment-service has been experiencing repeated OOMKills due to a memory leak, resulting in a high-urgency production incident with ID Q39OZFTVFHYSEF. The incident started at 2026-05-30T06:44:22Z and is currently affecting the payment-service, which is a critical component of the system. The error started after a recent deploy of the payment-service v2.4.3, which was deployed by @priya.dev.

ROOT CAUSE ANALYSIS:
The most likely cause of the incident is the recent deploy of the payment-service v2.4.3, specifically the PR "deps: upgrade stripe-python from 5.4.0 to 7.0.0" by @priya.dev, which is known to have a memory leak in stripe-python 7.0.0.

CORROBORATING EVIDENCE:
- The #deploys channel shows a recent deploy of the affected service within 2 hours of the incident
- The #incidents channel shows human responder observations and commentary, which also started after the deploy
- The #alerts channel shows automated monitoring alert messages indicating a connection pool exhaustion, high latency, and high error rate, all of which started after the deploy
- Sentry errors started at the same time as the deploy, indicating a possible correlation
- The Grafana dashboard shows memory issues and connection pool exhaustion

THIRD-PARTY STATUS:
There is no vendor outage contributing to this incident, as the Statuspage indicates that all systems are operational.

RECOMMENDED ACTIONS:
1. Roll back the recent deploy of the payment-service v2.4.3 to a previous version to see if it resolves the issue
2. Run the command "kubectl describe pod <pod_name>" to get more information about the pods that are experiencing the OOMKills
3. Check the Grafana dashboard for any other signs of memory issues or connection pool exhaustion
4. Investigate the recent changes to the payment-service code to identify the cause of the issue

CONFIDENCE LEVEL:
High, because the recent deploy of the payment-service v2.4.3 is the most likely cause of the incident, and the errors started at the same time as the deploy.

CORAL SQL QUERIES
------------------------------------------------------------
-- PAGERDUTY DATA
SELECT id, title, urgency, status, created_at
FROM pagerduty.incidents
WHERE id = 'Q39OZFTVFHYSEF'
LIMIT 5

-- GITHUB RECENT PRs (last 3 hours)
SELECT title, merged_at, user__login, html_url, additions, deletions
FROM github.pulls
WHERE owner = 'dhupthumbadiya2005'
  AND repo = 'demo-payment-service'
  AND merged_at >= '2026-05-30T06:44:22Z' - interval '3 hours'
ORDER BY merged_at DESC
LIMIT 10

-- SENTRY ERRORS
SELECT title, level, count, first_seen, last_seen
FROM sentry.issues
WHERE project = 'payment-service'
ORDER BY first_seen DESC
LIMIT 10

-- GRAFANA ALERTS
SELECT title, no_data_state, exec_err_state
FROM grafana.alert_rules
WHERE no_data_state != 'OK' OR exec_err_state != 'OK'
LIMIT 10
